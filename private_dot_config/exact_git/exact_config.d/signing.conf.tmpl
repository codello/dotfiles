{{ if .personal -}}
[credential]
	helper = osxkeychain

[gpg]
	format = ssh

[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
	allowedSignersFile = ../allowed_signers

[commit]
	gpgsign = true
{{- end }}
